<Window x:Class="SenzuraPOS_DAPP.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SenzuraPOS_DAPP"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Home - BYTEBUY POS" Height="500" Width="850"
        Icon="Assets/ico.ico"
        WindowStartupLocation="CenterScreen"
        Background="#f2f4f8">

    <Grid>
        <Border CornerRadius="12" Background="{DynamicResource MaterialDesignCardBackground}" Padding="30"
                HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Image Source="Assets/ico.png" Width="80" Height="80" Margin="0,0,0,20" HorizontalAlignment="Center" Grid.Row="0"/>

                <TextBlock Text="Welcome to BYTEBUY"
                           FontSize="30"
                           FontWeight="Bold"
                           Foreground="{DynamicResource PrimaryHueMidBrush}"
                           HorizontalAlignment="Center"
                           Margin="0 0 0 15"
                           TextAlignment="Center" Grid.Row="1"/>

                <TextBlock Text="Your Trusted Point of Sale System"
                           FontSize="16"
                           Foreground="{DynamicResource MaterialDesignBodyLight}"
                           HorizontalAlignment="Center"
                           Margin="0 0 0 25"
                           TextAlignment="Center" Grid.Row="2"/>

                <Button Content="Start Billing"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Width="220"
                        Height="50"
                        Background="{DynamicResource PrimaryHueMidBrush}"
                        Foreground="{DynamicResource PrimaryHueMidForegroundBrush}"
                        FontSize="16"
                        FontWeight="SemiBold"
                        Click="StartBilling_Click" 
                        Margin="0 10 0 0"
                        Cursor="Hand" Grid.Row="3"/>

                <Button Content="View Reports"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Width="220"
                        Height="50"
                        Background="{DynamicResource PrimaryHueMidBrush}"
                        Foreground="{DynamicResource PrimaryHueMidForegroundBrush}"
                        FontSize="16"
                        FontWeight="SemiBold"
                        Click="ViewReports_Click" 
                        Margin="0 10 0 0"
                        Cursor="Hand" Grid.Row="4"/>

                <Button Content="Manage Inventory"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Width="220"
                        Height="50"
                        Background="{DynamicResource PrimaryHueMidBrush}"
                        Foreground="{DynamicResource PrimaryHueMidForegroundBrush}"
                        FontSize="16"
                        FontWeight="SemiBold"
                        Click="ManageInventory_Click" 
                        Margin="0 10 0 0"
                        Cursor="Hand" Grid.Row="5"/>
            </Grid>
        </Border>
    </Grid>

</Window>
